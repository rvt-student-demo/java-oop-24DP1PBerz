package rvt;

import java.util.Scanner;

public class App {
    public static void main(String[] args) {
        TodoUI ui = new TodoUI();
        ui.show();
        // TodoList list = new TodoList();
        // Scanner scanner = new Scanner(System.in);
        // TodoUI user = new TodoUI(list, scanner);
        // user.start();
    }
}
